<%# Navigation bar at the top   %>

<%# Map with the start locations of each gruppettos %>
<div style="width:100%; height: 30vh;" class="sticky-top"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
<%# col-4 mid navigation with padding and space evenly %>
<div class="container">
  <div class="row py-2">
    <div class="col">
      Upcomming
    </div>
    <div class="col">
      Going
    </div>
    <div class="col">
      Past
    </div>
  </div>
</div>
<% @gruppettos.each do | gruppetto | %>
  <%# renders the index_card.html.erb with sending the gruppetto params %>
  <%= render "index_card", gruppetto: gruppetto %>

<% end %>
